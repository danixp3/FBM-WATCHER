# Copia como precio_selector.txt. Puedes poner VARIAS líneas de selector (orden = prioridad).
# Chrome: clic derecho en el precio → Inspeccionar → Copy → Copy selector.
#
# Por qué falla a veces:
# - #mount_0_0_xx cambia en cada carga o entre anuncios.
# - Las clases tipo x9f619 son generadas y Facebook las cambia entre vistas (móvil/desktop, A/B).
#
# Qué hace el script:
# - Prueba cada línea del fichero.
# - Para cada una: selector completo, el mismo sin #mount_..., y sufijos (últimos segmentos).
# - Si el selector corto coincide con varios nodos, elige el primero cuyo texto parezca un precio.
#
# Si un anuncio usa otro layout, añade una segunda línea con el selector copiado de ese layout.
# El script solo usa este fichero si el precio sigue vacío o claramente mal tras el extractor automático.

# span.ejemplo
# #mount_0_0_AB > div > ... > span
